let express,request,dotenv,connectDB;_992‍.w("express",[["default",["express"],function(v){express=v}],["request",["request"],function(v){request=v}]]);_992‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_992‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);



dotenv.config();
connectDB();
const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("Server is running...");
});

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => console.log(`Server is running on port ${PORT}`));
